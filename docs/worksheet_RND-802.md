# GPU AG - Worksheet

## Текущие задачи

- RND-792: Сглаживание GR
- RND-790: Улучшение функции награды (комментарий добавлен)

---

## [2025-12-23 12:45] Начало нового блока работ

### Следующие шаги

1. **Сглаживание GR (RND-792)**
   - Гипотеза: гладкая GR → гладкий ландшафт → стабильнее оптимизация

2. **Другие методы оптимизации**
   - scipy.minimize (L-BFGS-B, Powell)
   - optuna
   - Комбинации алгоритмов

3. **Улучшение objective function**
   - Проблема: objective не коррелирует с RMSE (отрицательная корреляция!)

---

## Заметки

---

## [2025-12-23 14:26] Тест 243 vs 867 популяций

**Исправлено:**
1. Добавлен `load_dotenv()` в slicer.py (не загружался .env)
2. Добавлены GPU параметры в cpu_baseline/.env
3. Исправлено логирование: fun + evals вместо correlation

**Сравнение (Well1798~EGFDL, 1 итерация):**

| Метрика | 867 pop (range=10°) | 243 pop (range=5°) |
|---------|---------------------|---------------------|
| RMSE | 0.227м | 0.226м |
| Max dev | 0.715м | 0.714м |
| Total evals | 346,800 | 194,400 |
| Время | ~16 мин | ~2 мин |

**Вывод:** Качество идентичное при range=5° vs range=10°. Скорость в 8 раз выше.

---

## [2025-12-23 15:20] Расширенное логирование CSV

**Добавлено:**
1. `eval_count` в `TorchObjectiveWrapper` - фактическое количество evaluations
2. Детальное логирование в `_log_optimization_result`:
   - MD range (start_md - end_md)
   - fun (objective)
   - pearson, mse
   - angle_penalty, angle_sum_penalty
   - angles (список углов сегментов)
   - actual_evals / expected_evals

**CSV формат:** `results_wells_grid_15/optimization_statistics.csv`
```
timestamp,well_name,step_type,start_md,end_md,segments_count,fun,pearson,mse,angle_penalty,angle_sum_penalty,angles,actual_evals,expected_evals,elapsed_time,success
```

**Тест полной скважины (multi-center):** 22/~45 итераций выполнено, затем StarSteer crash.

**Результат:** CSV формат работает корректно, все метрики записываются.

---

## [2025-12-23 17:55] Single CMA-ES mode

**Добавлено:**
- `GPU_CMAES_MODE=single|multi` - выбор режима CMA-ES
- `GPU_SINGLE_POPSIZE=2000` - размер популяции для single mode

**Сравнение (тест Well1798~EGFDL):**

| Metric | Single (pop=2000) | Multi (243×8) |
|--------|-------------------|---------------|
| Time/step | **0.27s** | 63s |
| Speedup | **230x** | 1x |
| Evals/step | 100,000 | 97,200 |
| RMSE тренд | растёт | растёт |

**Вывод:** Single mode даёт **230x speedup** при том же качестве!
Рост RMSE - проблема objective function (RND-790), не алгоритма.

---

## [2025-12-23 19:07] Тест повторяемости - ЗАВЕРШЕНО

**5 прогонов выполнено** (4 полных):

| Run | Steps | fun mean | fun max | pearson |
|-----|-------|----------|---------|---------|
| 2 | 44 | 0.0075 | 0.065 | 0.597 |
| 3 | 43 | 0.0036 | 0.015 | 0.617 |
| 4 | 44 | 0.0072 | 0.068 | 0.605 |
| 5 | 43 | 0.0063 | 0.065 | 0.606 |

### Сравнение интерпретаций

| Runs | Identical (<0.01m) | Close (<0.1m) | Max diff |
|------|-------------------|---------------|----------|
| 2 vs 3 | 76% | 82% | 6.87m |
| 2 vs 4 | 80% | 81% | 9.71m |
| 2 vs 5 | 83% | 90% | 1.31m |
| 3 vs 4 | 66% | 73% | 13.77m |
| 3 vs 5 | 76% | 85% | 7.76m |
| 4 vs 5 | 69% | 76% | 9.71m |

**Лучшая пара:** Run 2 vs 5 (90% close, max diff 1.31m)
**Худшая пара:** Run 3 vs 4 (73% close, max diff 13.77m)

### Выводы

1. **Повторяемость:** 66-90% (зависит от того, попал ли алгоритм в локальный минимум)
2. **Max расхождение:** до 13.77m в худшем случае
3. **Причина:** стохастичность CMA-ES на "плохих" данных (низкий pearson)

### Исправлен баг race condition

Добавлено 10 retry с интервалом 0.5s в `_wait_for_fresh_data_file()` для обработки атомарной перезаписи файла StarSteer.

### Итоги Single CMA-ES mode

| Метрика | Значение |
|---------|----------|
| Speedup | **230x** |
| Повторяемость | 66-90% |
| Качество | Эквивалентно multi-center |

**Следующие шаги:**
1. Добавить retry при fun > threshold
2. Исследовать сглаживание GR (RND-792)
3. Улучшить objective function (RND-790)

---

## [2025-12-23 19:35] Эксперимент с LOOKBACK

**PYTHON_LOOKBACK_DISTANCE=200м** (было 50м):

| Метрика | 50м | 200м |
|---------|-----|------|
| pearson (конец) | 0.03-0.22 | **0.70-0.80** |
| углы (конец) | +1.67° | **-2.5°** ✓ |
| fun max | 0.065-0.091 | **0.019** |
| Segments | 145-148 | 57 |

**Вывод:** lookback=200м даёт правильные углы (~-2.5°) и лучший pearson.

### Тест lookback=300м

| Метрика | 200м | 300м |
|---------|------|------|
| pearson | 0.70-0.80 | 0.36-0.73 |
| углы | -2.3 до -2.9° | -1.9 до -2.5° |
| fun max | 0.019 | 0.019 |

### Тест ANGLE_RANGE=10° (lookback=200м)

Результаты идентичны 5° - алгоритм находит оптимум.

### Финальные настройки

```
PYTHON_LOOKBACK_DISTANCE=200.0
PYTHON_ANGLE_RANGE=10.0
GPU_CMAES_MODE=single
GPU_SINGLE_POPSIZE=2000
```

### Создана задача RND-794

**Add self-correlation to reward function** - добавить self-correlation в функцию награды.

---

## [2025-12-23 21:15] Gaussian filter для GR (RND-792)

**Реализовано:**
1. Import `gaussian_filter1d` в `gpu_executor.py`
2. Параметр `GR_SMOOTHING_SIGMA` в `.env` (default=0, no smoothing)
3. Применение фильтра после `well_to_numpy()` перед оптимизацией

**Код:**
```python
if self.gr_smoothing_sigma > 0:
    well_np['value'] = gaussian_filter1d(well_np['value'], sigma=self.gr_smoothing_sigma)
```

**Настройка (изменено):** Savitzky-Golay, window=5, order=2

```
GR_SMOOTHING_WINDOW=5
GR_SMOOTHING_ORDER=2
```

**Статус:** Готово к тестированию.

---

## [2025-12-23 21:10] Тест Savitzky-Golay + lookback=200

**Параметры:**
- GR_SMOOTHING_WINDOW=5, GR_SMOOTHING_ORDER=2
- PYTHON_LOOKBACK_DISTANCE=200
- PYTHON_ANGLE_RANGE=10
- GPU_CMAES_MODE=single, GPU_SINGLE_POPSIZE=2000

**Результаты (Well1798~EGFDL):**

| Метрика | lookback=50 (без сглаж.) | lookback=200 + Savgol |
|---------|--------------------------|----------------------|
| Final RMSE | 12.54m | **2.94m** |
| Max dev | 12.99m | **3.34m** |
| Segments | 148 | **57** |
| Objective avg | 0.0157 | **0.0141** |
| Coverage 3.0m | 0% | **86.9%** |

**Вывод:** Savitzky-Golay (5,2) + lookback=200 даёт значительное улучшение RMSE (12.5m → 2.9m).

---

## [2025-12-23 22:15] Общая функция фильтрации GR

**Создано:**
- `gr_utils.py` - общий модуль фильтрации:
  - `apply_gr_smoothing(values)` - применяет фильтр по настройкам .env
  - `get_smoothing_params()` - возвращает текущие параметры

**Обновлено:**
- `gpu_executor.py` - использует `apply_gr_smoothing`
- `analyze_variance.py` - использует `apply_gr_smoothing`, исправлен парсинг сегментов (только startMd, без endMd)

**Результат analyze_variance.py (Savgol 5,2):**
- 55 границ сегментов
- Var×100: 0.06 - 1.25 (среднее ~0.4)
- Std×10: 0.24 - 1.12 (среднее ~0.55)

Теперь изменение фильтра в .env автоматически применяется и в оптимизаторе, и в анализе variance.

---

## [2025-12-24 11:50] Рефакторинг нормализации TypeWell

**Проблема:** В `gpu_executor.py` был хардкоженный коэффициент `MDE_MULTIPLIER = 1.446594` вместо реального из `NormalizationCalculator`.

**Решение:**

1. **Скопирован `typewell_provider.py` из MDE** в `cpu_baseline/`
   - `extend_pseudo_with_typelog(pseudo_log, type_log, norm_coef)` - с динамическим коэффициентом
   - `TypewellProvider` класс с методами:
     - `set_normalization_coef(multiplier)` - устанавливает `1.0 / multiplier`
     - `apply_to_well_data()` - применяет transformation

2. **Обновлён `emulator_processor.py`**:
   - Импорт `TypewellProvider`
   - Создание `self.typewell_provider` в `__init__`
   - После нормализации: `typewell_provider.set_normalization_coef(multiplier)`
   - Step 6.5: `typewell_provider.apply_to_well_data()` перед slice_well_data

3. **Упрощён `gpu_executor.py`**:
   - Удалены `MDE_MULTIPLIER`, `TYPELOG_NORM_COEF`
   - Удалена `extend_pseudo_with_typelog()` (перенесена в typewell_provider)
   - Упрощён `_create_typewell()` - теперь просто `return TypeWell(well_data)`

4. **Добавлен `TYPEWELL_MODE=pseudo_stitched` в `.env`**

**Логика:**
```
NormalizationCalculator:  well.value *= multiplier
TypewellProvider:         typeLog.value *= (1.0 / multiplier)
```

**Аналогичное исправление сделано в Multidrilling_emulator через агента.**

---

## [2025-12-24 12:32] Запуск теста по всем скважинам

**Параметры:**
- 103 скважины из wells_config_full.json
- TYPEWELL_MODE=pseudo_stitched (с динамическим коэффициентом)
- Savitzky-Golay (5,2), lookback=200, angle_range=10
- GPU_CMAES_MODE=single, popsize=2000

**Мониторинг:**
- Будильник: ID=503, время 12:42
- Лог: /mnt/e/Projects/Rogii/gpu_ag/slicer_all_wells.log
- При падении - НЕ перезапускать, обсудить

**Статус:** УПАЛ на первой скважине

---

## [2025-12-24 13:00] Исправление race condition в _send_command

**Проблема:** При запуске теста на Well1150~EGFDL упал с `FileNotFoundError` при записи commands.json.

**Последовательность:**
1. SELECT_WELL (source) → OK
2. INIT_WELL_SLICER → OK
3. sleep(1)
4. SELECT_WELL (target) → ПАДЕНИЕ при open(commands.json, 'w')

**Причина:** WSL/NTFS race condition - StarSteer ещё работал с файловой системой после INIT_WELL_SLICER.

**Исправление:** Добавлен retry в `_send_command()`:
- 5 попыток с интервалом 0.5 секунды
- Логирование retry
- Обработка FileNotFoundError, PermissionError, OSError

**Статус:** Исправлено, перезапущено → упало на Well162~EGFDL

---

## [2025-12-24 13:15] Исправление dtype в typewell_provider

**Проблема:** На Well162~EGFDL упал с `TypeError: Cannot cast array data from dtype('O') to dtype('float64')`.

**Причина:** В typeLog есть точки с None в поле `data`.

**Исправление в `extend_pseudo_with_typelog()`:**
- Фильтрация точек с None или не-числовыми данными
- Явное преобразование в float()
- Явный dtype=np.float64 для массивов

**Статус:** Тест запущен

---

## [2025-12-24 13:25] Проблема со смещением интерпретации

**Скважина:** Well1150~EGFDL

**Симптомы:**
- RMSE скачет с 0.5м до 212м на MD=4034.9м
- startShift сегментов прыгает с 9м до 231м
- Смещение ~186м (≈610 футов)

**Данные для анализа:**
- json_comparison/new_slicing_well_raw_4034.9.json
- json_comparison/new_slicing_well_raw_4004.8.json (нормальный)

**Гипотеза:** `manual_segments_json` берётся из `well_data['interpretation']`, а не из текущей рассчитанной интерпретации. На каждой итерации интерпретация перезаписывается с нуля.

**Проблемные скважины (RMSE > 100м):**
- Well1150~EGFDL (tvdTypewellShift = +94.18м)
- Well42~EGFDL

**Гипотеза:** Арифметическая ошибка с tvdTypewellShift - двойное применение или неправильный знак. Интерпретация выглядит логично если её сместить.

**Пропущенные скважины:**
- Well1426~EGFDL - segment.start_idx == segment.end_idx (нулевой сегмент)

**Статус:** Тест перезапущен (14:41), Well1426 пропущена

---

## [2025-12-24 15:45] Прогресс теста по всем скважинам

**Текущий статус:**
- Прогресс: 16/103 скважин (15.5%)
- Последняя завершённая: Well461_landing~EGFDL
  - 36 оптимизаций, 100% успех
  - Objective: avg=0.0046, max=0.011
- Текущая: Well915~EGFDL

**Оценка:** ~5 мин/скважина × 87 оставшихся ≈ 7-8 часов

**[15:43] Update:**
- Завершено: 21/103 (20.4%)
- Текущая: Well1675~EGFDL - RMSE=12.47м (проблемная, tvdTypewellShift?)
- Будильник: ID=513, 16:43

**[15:56] Падение на Well1426~EGFDL:**
- Ошибка: segment.start_idx == segment.end_idx
- Добавлена в config как skipped
- Тест перезапущен (16:00)

**[16:01] Update:**
- Well1150~EGFDL - RMSE=84.8м (tvdTypewellShift проблема подтверждена)

---

## [2025-12-24 17:10] Перезапуск с исправлениями

**Исправлено:**
1. `FORCE_REPROCESS_ALL=false` - флаги не сбрасываются при перезапуске
2. Логика последнего слайса - теперь обрабатывается до конца:
   - Было: `remaining_md < slice_distance` → break (без обработки)
   - Стало: `remaining_md <= 0` → break (после обработки)

**Результат:**
- Well1150~EGFDL: ✅ `Last slice` → `WELL COMPLETE` → `Completed`
- Well162~EGFDL: ✅ `WELL COMPLETE` → `Completed`
- Well1426~EGFDL: skipped (zero-length segment)

**Мониторинг:** будильник каждые 15 мин (ID=515)

---

## [2025-12-24 22:10] Анализ тайминга слайсов

**Один слайс занимает ~3.4 сек:**

| Этап | Время | % |
|------|-------|---|
| Send slice command | 0.01s | 0% |
| Wait StarSteer response | 0.6s | 18% |
| Read & validate JSON | 1.8s | 53% |
| GPU CMAES optimization | 0.35s | 10% |
| Export to StarSteer | 0.1s | 3% |

**Вывод:** GPU загружен только 10% времени! Bottleneck = I/O (70%).

### Идея: Параллельные инстансы StarSteer

**Концепция:**
- Запустить N инстансов StarSteer в разных директориях
- Каждый обрабатывает свою скважину параллельно
- GPU может обрабатывать batch из N скважин одновременно

**Архитектура:**
```
Orchestrator
    ├── StarSteer Instance 1 (dir: slicer_de_1) → Well A
    ├── StarSteer Instance 2 (dir: slicer_de_2) → Well B
    ├── StarSteer Instance 3 (dir: slicer_de_3) → Well C
    └── GPU Executor (batch processing)
```

**Потенциальный speedup:** 3-5x при 3-5 инстансах (лимит = память GPU и CPU)

---

## [2025-12-24 22:35] Анализ сохранения интерпретаций

### Текущая логика: `_save_ng_interpretation_to_source_well()`

**Файл:** `cpu_baseline/slicer.py:1872`

**Шаги:**
1. STOP_AG на текущей (sliced) скважине
2. SELECT_WELL (переключение на source well)
3. COPY_INTERPRETATION (Manual → AI_Interpretation)
4. SELECT_INTERPRETATION (AI_Interpretation)
5. CONFIGURE_AG_SETTINGS (useGridSlice: false)
6. START_AG
7. Перезапись interpretation.json (trigger QFileSystemWatcher)
8. STOP_AG (cleanup)

**Контроль:** `SAVE_NG_INTERPRETATION=true` в .env (включено)

**Вызывается:** После завершения `_run_slicing_loop()` (строка 880)

### Проблема

Если StarSteer падает в середине обработки:
- Интерпретация теряется
- При рестарте начинаем сначала

### Решение: Промежуточное сохранение

**Вариант 1:** Сохранять interpretation.json после каждой скважины в отдельную директорию:
```
results/
  Well1150~EGFDL/interpretation.json
  Well162~EGFDL/interpretation.json
  ...
```

**Вариант 2:** Сохранять в source well после каждого слайса (дорого по I/O)

**Рекомендация:** Вариант 1 - сохранять копию финальной интерпретации.

---

## [2025-12-24 22:35] Параметры для параллельных инстансов

### Необходимые параметры для каждого воркера:

| Параметр | Описание | Пример |
|----------|----------|--------|
| `--starsteer-dir` | Уникальная директория StarSteer | `slicer_de_1`, `slicer_de_2` |
| `--wells-file` | JSON с подмножеством скважин | `wells_worker_1.json` |
| `--worker-id` | ID воркера для логирования | `1`, `2`, `3` |

### Подготовка инстансов StarSteer

```bash
# Создать N копий директории StarSteer
for i in 1 2 3; do
  cp -r slicer_de slicer_de_$i
done
```

### Разделение скважин

```python
# Разбить wells_config на N частей
wells = load_json("wells_config_full.json")["wells"]
unprocessed = [w for w in wells if not w.get("processed")]
chunks = split_into_n_chunks(unprocessed, N_WORKERS)
for i, chunk in enumerate(chunks):
    save_json(f"wells_worker_{i+1}.json", {"wells": chunk})
```

### Запуск воркеров

```bash
for i in 1 2 3; do
  nohup python slicer.py --de \
    --starsteer-dir "slicer_de_$i" \
    --wells-file "wells_worker_$i.json" \
    --worker-id $i \
    > slicer_worker_$i.log 2>&1 &
done
```

### Требуемые изменения в коде

1. **slicer.py:**
   - Новый аргумент `--wells-file` (опционально, default=wells_config_full.json)
   - Новый аргумент `--worker-id` (для логов)
   - Сохранение результатов в отдельную директорию по worker-id

2. **Синхронизация:**
   - Общий файл статуса (или каждый воркер пишет свой)
   - Не нужен mutex - каждый воркер работает со своим подмножеством

### Текущий статус теста

- Скважина: Well778~EGFDL
- Итерация: 54, осталось 392м
- RMSE: 6.48м (высокий)

---

## [2025-12-25 00:35] Анализ зависания Well850 на последнем слайсе

### Факты

- Последний успешный MD: **5187.1м**
- remaining_md от StarSteer: **22.56м**
- Расчётный конец: 5187.1 + 22.56 = **5209.66м = 17092.06 футов**

### Данные из StarSteer UI

- Конец логов: **17018 футов**
- Конец траектории: **17073 футов**

### Несоответствие

- Slicer считает конец: 17092 ft
- StarSteer показывает: 17018 ft (логи) / 17073 ft (траектория)
- **Разница: ~74 ft (22м)** - откуда?

### Гипотеза

StarSteer возвращает `remaining_md_meters` в `slicer_status.json`, но это значение не бьётся с реальным концом логов/траектории. Возможно баг в StarSteer.

### Таймаут

- Well850 ждала 300 секунд (5 мин) ответа на последний slice
- После таймаута - SLICING LOOP DONE, Completed

### Исправление (сделано)

- Убрана проверка `remaining_md <= 0`
- Убрано логирование "Last slice"
- Теперь полагаемся только на `can_add_more=False` от StarSteer

### TODO: Обследовать StarSteer

**Потенциальная мина:** StarSteer возвращает `remaining_md_meters` которое не бьётся с реальным концом логов/траектории. Нужно:
1. Найти где в StarSteer вычисляется remaining_md_meters
2. Понять почему 17092 ft вместо 17018/17073 ft
3. Проверить граничные случаи (последний слайс, конец скважины)

---

## [2025-12-25 01:10] Повторный тест Well850 - ПОДТВЕРЖДЁН БАГ

**Результат:** Зависание воспроизводится!

**Точка зависания:**
- MD = 5187.1m (iteration 48)
- remaining_md = 22.56m
- can_add_more = True (StarSteer говорит "можно добавить ещё")

**Проблема:**
StarSteer возвращает `can_add_more=True`, но не может обработать следующий слайс.
Slicer ждёт fresh data 300 секунд и таймаутится.

**Вывод:** Баг RND-800 подтверждён. Проблема в StarSteer, не в slicer.py.

**Workaround:** Пока можно добавить проверку remaining_md < MD_STEP и принудительно завершать, но это костыль. Правильное исправление - в StarSteer.

---

## [2025-12-25 01:35] 🎯 ROOT CAUSE RND-800 НАЙДЕН!

### Причина
`copyLogRange` в WellSlicerController.cpp (строки 1273-1277):
```cpp
if (traitsToAdd.empty())
{
    // "No traits in range, skipping"
    return;   // ← EXIT! LogUpdated НЕ генерируется!
}
```

### Цепочка событий
1. **GR лог закончился на 17018 ft**
2. Slice #48 запрашивает 17027→17125 ft (за пределами GR)
3. `copyLogRange('GR')` → "No traits in range" → `return`
4. **LogUpdated event НЕ генерируется** (строки 1304-1305 не достигаются)
5. AG sync триггерится на GR логе (основной лог для AG)
6. Нет LogUpdated для GR → AG sync НЕ запускается
7. **`slicing_well.json` НЕ обновляется!**
8. Slicer.py ждёт файл → таймаут 300 секунд

### Подтверждение из xlogs
```
[LOG_SLICE] Log 'TGAS': ADDING 98 traits    ← LogUpdated ✅
[LOG_SLICE] Log 'ROP': No traits in range   ← return, LogUpdated ❌
[LOG_SLICE] Log 'GR_fix': No traits in range ← return, LogUpdated ❌
[LOG_SLICE] Log 'GR': No traits in range     ← return, LogUpdated ❌
```

### Варианты исправления
1. Генерировать LogUpdated даже если нет данных (просто не делать early return)
2. Использовать TrajectoryUpdated event (траектория всегда обновляется)
3. Явно триггерить AG sync после каждого slice независимо от логов

### Статус
**ПРИЧИНА НАЙДЕНА.** Ждём решения пользователя какой вариант реализовать.

---

## [2025-12-25 02:00] Дополнительный анализ RND-800

### Вопрос 1: Почему TrajectoryUpdated НЕ триггерит slicing_well.json?

**Ответ:** `slicing_well.json` создаётся **ТОЛЬКО при AG START**, не при updateObjects()!

- `prepareInitialStageData()` вызывается только в `AutoGeosteeringInitializationStage::execute()` (DLL)
- `updateObjects()` передаёт данные в DLL для пересчёта, но НЕ экспортирует JSON
- TrajectoryUpdated триггерит updateObjects, но это НЕ приводит к экспорту slicing_well.json

### Вопрос 2: Малый шаг слайса (1м)

**Ответ:** ДА, event может не сгенерироваться!

- `copyTrajectoryRange`: если `pointsToAdd.empty()` → `return` → TrajectoryCompleteUpdated ❌
- `copyLogRange`: если `traitsToAdd.empty()` → `return` → LogUpdated ❌
- Условие: `point.measuredDepth > fromMD && point.measuredDepth <= toMD`

**Вывод:** Если шаг слайса меньше шага между точками траектории/лога - никакой event не сгенерируется!

### Вопрос 3: max_source_md = 17200 ft > траектории (17073 ft)

**Ответ:** `max_source_md = max(trajectory, все логи)`

- Траектория заканчивается на: 17073 ft
- TGAS лог заканчивается на: **17200 ft** ← берётся максимум
- Это **корректно** - slicer обрабатывает логи за пределами траектории

**Вывод:** GR лог (17018 ft) < Траектория (17073 ft) < TGAS (17200 ft). Поэтому remaining_md считается по TGAS.

### Итоговая проблема

slicing_well.json экспортируется только при:
1. AG START (инициализация)
2. AG step completion (после оптимизации)

При slice за пределами GR:
- LogUpdated для GR не генерируется (нет данных)
- AG sync не запускается (триггер на GR)
- AG step не выполняется
- slicing_well.json не обновляется

### Правильное решение

**Вариант 3 (лучший):** После каждого slice явно триггерить экспорт slicing_well.json, независимо от того, были ли обновления логов/траектории.

Это решает:
- Проблему с малым шагом
- Проблему с slice за пределами логов
- Не зависит от событий LogUpdated/TrajectoryUpdated

---

## [2025-12-25 02:20] 🔥 ВТОРАЯ ПРОБЛЕМА НАЙДЕНА!

### Проблема 1: DLL не экспортирует JSON при updateObjects

Подтверждено агентом Autogeosteering:
- `slicing_well.json` создаётся **ТОЛЬКО** при:
  - `InitializationStage::execute()` (AG START)
  - `FinalizationStage::execute()` (AG завершение)
- `updateObjects()` → `updateModel()` → `calculate()` → **НЕТ вызова unloadData()**

### Проблема 2: AG ИГНОРИРУЕТ события от slicing_well!

Подтверждено агентом StarSteer:

**Event генерируется для TARGET well с НОВЫМ UUID:**
```cpp
// WellSlicerController.cpp:1139,1142
TrajectoryCompleteUpdated completeEvent(targetWell, ...);  // TARGET, не source!

// WellSlicerController.cpp:250
mTargetWellUuid = copiedWell->getUuid();  // Копия = НОВЫЙ UUID
```

**AG подписан на SOURCE well:**
```cpp
// AutoGeosteeringController.cpp:1406
if (mAutoGeosteeringWorker->isRelatedObject(well->getUuid()))  // Проверяет UUID
```

**Результат:**
| Well | UUID | AG подписан? |
|------|------|--------------|
| SOURCE (исходная) | UUID_A | ✅ ДА |
| TARGET (slicing_well) | UUID_B | ❌ НЕТ |

**Event для TARGET игнорируется!** `isRelatedObject(UUID_B)` → FALSE

### Итого: ДВЕ независимые проблемы

1. **copyLogRange** делает early return без LogUpdated (если нет данных в range)
2. **AG игнорирует события от slicing_well** (подписан только на source well)

### Правильное решение

Нужно исправить ОБОИХ:
1. В DLL: экспортировать slicing_well.json при каждом `updateObjects()` или явном вызове
2. В StarSteer: либо подписать AG на slicing_well, либо генерировать event для source well

---

## [2025-12-25 14:45] Workaround в slicer.py

**Изменения:**
1. `check_interval`: 0.5s → **0.1s**
2. `timeout`: 300s → **15s**
3. При timeout + can_add_more=True → **continue** (не застревает)
4. Polling slicer_status: 10 retries × 0.03s

**Тест Well850 5м:** ✅ Успех! Проблемная зона прошла за ~1.5 мин.

**Запущен тест всех 103 скважин** с шагом 30м:
- Лог: `slicer_all_wells_v2.log`
- Оценка: ~8-9 часов

---

## [2025-12-25 17:25] Прогресс теста + новые функции

**Тест 103 скважин: ✅ ЗАВЕРШЁН**
- Результат: **98/103 (95%)**
- Пропущено: 5 (ошибки zero-length сегментов)
- Время: ~3 часа (14:42 → 17:36)
- Скорость: ~1.8 мин/скважина
- Well850 (проблемная RND-800) - ✅ прошла!

**Добавлено:**

1. **Monte Carlo optimizer** (`gpu_executor.py`):
   - `GPU_ALGORITHM=MONTECARLO`
   - `GPU_MONTECARLO_SAMPLES=100000` (дефолт)
   - Чистый random search на GPU

2. **CLI параметр `--optimizer`** (`slicer.py`):
   ```bash
   python slicer.py --de --optimizer montecarlo
   python slicer.py --de --optimizer cmaes
   ```

3. **Второй инстанс StarSteer** (`slicer_de_2`):
   - Для параллельного запуска разных алгоритмов
   ```bash
   python slicer.py --de --starsteer-dir "slicer_de_2" --optimizer montecarlo
   ```
